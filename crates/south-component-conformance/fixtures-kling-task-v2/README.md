# Kling task-v2 candidate fixtures

These are host-code transcriptions, not upstream recordings. They freeze the
candidate v2 translation only; v1 and server production dispatch are unchanged.

The request carries an explicit operation and the real upstream model. Mode
policy, pricing and reservation estimates remain host responsibilities. Omni
requires a resolved mode; motion preserves the existing wire mode `pro`.
The locator records the relative creation route, independently of model names.

Candidate tightening requiring confirmation before production migration:
any malformed artifact in a list makes the entire observation Unknown. Missing
or empty URLs and object/array id or duration values are not filtered or coerced.
The component preserves scalar artifact metadata and both usage facts; invalid
meters make the observation Unknown, while missing/null meters remain unavailable rather than zero. A failed query or ambiguous create response
never proves that work ended. Frozen expected outputs were authored from the
host behavior and these explicit candidate decisions, not generated by invoking
the reference implementation.
