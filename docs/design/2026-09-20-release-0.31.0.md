# v0.31.0 百炼视频发布准备

基线 v0.30.0/fb0c4fd 已正式发布六包；本次新增 managed 百炼视频第七包。
所有行为先以开发版本完成原生/真实 Wasm 对拍，后统一候选身份；当前未发布。

| 包 | 0.30 已发布身份 | 0.31 候选身份 |
|---|---|---|
| provider-openai-compatible | 2.1.2 | 2.1.3 |
| provider-anthropic | 1.0.3 | 1.0.4 |
| provider-gemini | 1.1.2 | 1.1.3 |
| task-kling | 1.0.2 | 1.0.3 |
| task-kling-v2 | 0.30.0 | 0.31.0 |
| task-minimax-v2 | 0.30.0 | 0.31.0 |
| task-bailian-v2 | 未发布 | 0.31.0 |

workspace 八 crate、各 manifest 的 south_runtime、兼容清单 release 一起升级。
HTTP9、Task5、auth4、error2、stream2、kernel/IR/WIT 均不变，无新 ABI 能力。
锁文件仅改本仓 crate 和各 guest 的自身版本；不执行全量依赖升级。
release 工作流只增加第七包 build/package，触发与发布权限不变。

严格元组继续拒绝历史 runtime；旧任务需排空或交旧宿主，不声称保留包即可跨
runtime执行。原生身份也不能以当前版本静默代换历史 revision。两个宿主的 task
采用记录维持原状，库验收与某一宿主 M2 生产采用分别交付。

验收：发行身份守卫先真实 RED，随后七包元组/元数据一致；README 16 项短检查，
含七个真实 guest、无默认特性、边界、供应链和 fuzz 工作区编译。没有长 fuzz/soak。
实际日志与失败修复另记验证记录；通过前不声称发布就绪，不打 tag 或 push。

## 发布前最终验证

独立只读审查发现的请求零时长／超范围导致宿主 500 已先红后绿修复并复核关闭。
修后 README 16 项全部通过；全功能 676/676（10.731 秒），无默认特性 652/652。
含七种真实 Wasm、冻结夹具、包身份、依赖边界、fuzz 编译和供应链检查。
日志索引 `/tmp/target-architecture-m2-south-final-v2-results.json`。

宿主候选实际 Wasm 测试余额／配额各 3/3，8.395／8.699 秒，覆盖七方法对照、
202 后 SIGKILL→持久 pin 恢复结算、缺原包保留资金且拒绝原生回退。此时依赖为
明确标记的开发覆盖；正式发布包及 server 全量矩阵仍需另验。

首次环境验证失败保留 `/tmp/target-architecture-m2-south-final-03.log`：PATH
选到 Homebrew Rust，Wasm 子构建无法找到 rustup 已安装的 target。后改用 rustup
shim，撤销本工作树 target 符号链接并以独立目录验证；不删除原共享缓存。
此前修前 16 项通过记录不能替代本段修后验证。
